#include <iostream>

int main() {

    // integers - whole numbers
    int age = 21;
    int year = 2024;
    int days = 4.5;

    std::cout << "Ramesh is " << age << " years old in " << year << " and today is his " << days << "th day.";
    // Now we have problem that integer can only store one value and not point.
    return 0;
}
